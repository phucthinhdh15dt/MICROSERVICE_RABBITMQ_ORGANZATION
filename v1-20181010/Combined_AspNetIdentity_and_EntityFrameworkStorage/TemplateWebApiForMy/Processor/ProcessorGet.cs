using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace TemplateWebApiForMy.Processor
{
    public static class ProcessorGet
    {



        //"getAll","filter","paging","sortby","single"
        //+getall->sortby
        //filter -sortby
        //paging->sortbyc
        //public static string MethodGetDefaute(Dictionary<string, string> condition)
        //{
        //    if (condition.Count == 0)
        //    {

        //    }
        //}
        //public static string ProcessorHttpGet(Dictionary<string, string> condition )
        //{
        //    if (
        //            (condition["getAll"]!= null )|| (condition["getAll"] != "")
        //            && ((condition["sortby"] != null) || (condition["sortby"] != ""))
        //            && ((condition["filter"] == null) || (condition["filter"] == ""))
        //            && ((condition["paging"] == null) || (condition["paging"] == ""))
        //            && ((condition["single"] == null) || (condition["single"] == ""))
        //       )
        //        {
        //            return 
        //        }
        //}
    }
}
